# Twitter remote MCP server (keyless) — manifest §5, §12

# Bu remote MCP server'ın dinlediği port (MCP Streamable HTTP transport)
PORT=4022
# MCP endpoint yolu
MCP_PATH=/mcp

# Proxy'lediği x402 merchant (twitter-backend)
TWITTER_BACKEND_URL=http://localhost:4021
