# Mock Twitter remote MCP server (manifest §5, §12)

# Port this remote MCP server listens on (MCP Streamable HTTP transport)
PORT=4022

# The x402 merchant this MCP proxies to (twitter-backend)
TWITTER_BACKEND_URL=http://localhost:4021
